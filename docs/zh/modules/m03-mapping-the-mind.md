# M03 Mapping the Mind

## 目标

把“feature 存在”推进到“feature 可以被成体系地编目、分组和浏览”。

## 核心参考

- [Mapping the Mind of a Large Language Model](https://www.anthropic.com/research/mapping-mind-language-model)

## 重点观察

- feature discovery 真正有用，是因为你能组织很多 feature，而不是只找到几个漂亮例子。
- catalog 不是神经元清单，而是行为模式的可搜索索引。
- 覆盖率一旦提升，你对模型分析的默认视角也会改变。

## 当前状态

- 在严格实时规则下是 `reading-only`
- 当前仓库不再提供这里的 live notebook，因为那会依赖预计算 catalog artifact。
- 共享参考 artifact：`artifacts/m03_feature_catalog.json`

## 验收题

- 为什么 feature catalog 会把 interpretability 从“挑几个酷例子”推进到“做地图”？
- 如果让你扩展这个教学版 catalog，你最想补哪一类 feature，为什么？
- 即使你展示了很多 feature 卡片，哪些更强的研究结论仍然还不能成立？

## 模块结论

这篇论文把 interpretability 从零散轶事推进到 feature 地图，但在当前仓库里它只保留为阅读与批判入口。
